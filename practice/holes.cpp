#include <bits/stdc++.h>

using namespace std;

#define ll long long
#define vll vector<ll>
#define pb push_back
#define mp make_pair

const int MAX_SIZE = 1e5+10;

vll pow(MAX_SIZE);
vector<pair<ll, ll > > count_next;

void pre(int n)
{
	int k = sqrt(n);
	for(int i=1; i<=n; i++)
	{

		
	}
}

int main()
{
	ios_base::sync_with_stdio(false);
	int n, m;
	cin >> n >> m;
	count_next.pb(mp(0L, 0L));
	for(int i=1; i<=n; i++)
		cin >> pow[i];

	return 0;
}