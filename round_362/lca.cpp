#include <bits/stdc++.h>
#define parent(i) i>>1
using namespace std;

typedef long long ll;

unordered_map<ll, ll> map1; 

ll lca(ll v, ll u){
	
}

int main(){
	ios_base::sync_with_stdio(false);
	int q;
	cin>>q;
	int choice;
	int v, u, w;
	while(q--){
		cin>>choice;
		if(choice == 1)
	}
	return 0;
}